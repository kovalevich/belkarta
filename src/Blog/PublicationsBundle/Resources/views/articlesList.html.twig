{% if items|length > 0 %}
    <ul>
        {% for item in items %}
            <li><a href="{{ path('publication_view', {slug: item.slug}) }}">{{ item.title }}</a></li>
        {% endfor %}
    </ul>
{% else %}
    Нет публикаций
{% endif %}